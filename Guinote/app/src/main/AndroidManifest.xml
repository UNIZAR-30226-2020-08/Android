<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.guinote">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TorneoViewHolder.BracketsCellViewHolder"></activity>
        <activity android:name=".Torneo" />
        <activity android:name=".carga_registro" />
        <activity android:name=".Pantalla_app" />
        <activity android:name=".navegacion_inicio">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PantallaJuego" />
        <activity android:name=".PantallaJuego1vs1" />
    </application>

</manifest>